# 注意事项

- 先按范围加载：本文件、`architecture/INDEX.md`、目标包页、`requirements/CONTEXT.md`，再读直接相关的领域上下文。
- 当前代码与已确认决定优先于这里的记录；只在这里保留稳定规则、边界、原因和不易从环境取得的约束。
- `pi-lsp-feedback` 的行为范围是诊断反馈：包内携带 Node LSP，可信项目可按需托管安装 `gopls`；它不执行格式化、修复、项目扫描或源码修改。
- 新增跨包事实前先确定语义所有权；共享事实只能有一个权威位置。

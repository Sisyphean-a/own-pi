# 2026-09

- 2026-09-20 · [演进] 思考中动图先按用户要求从紧凑 footer 隐藏（连同只为它存在的 125ms 帧计时器与四个事件装配一起移除），同日改为输入框上边框里 `Working…` 后面的纯图标，且只在模型生成期间出现。范围：package:pi-tui-enhancements、context:tui-experience
  原因：用户先要求隐藏页脚标记，随后要求改成“working 后面的一个图标”、不要文案、要会动，并确认采用“思考中门控 + 配色跟随思考等级”。新位置不自己持有计时器：帧索引由渲染时刻推导，重绘复用 Pi Loader 自身的帧计时器（默认 80ms 每帧 requestRender）；显示时机由 `turn_start` / `tool_execution_start` / `turn_end` / `agent_settled` / `session_shutdown` 驱动的思考状态决定，跑工具时只剩 `⠋ Working…`；颜色直接读 `editor.borderColor`（Pi 在 `thinking_level_changed` 时原地更新，与 spinner、边框同源），无需自跟踪等级；图标先占 2 列再交给 Pi 排版，宽度不足时退化为纯状态文本。实测单帧增量约 0.4µs（整帧 0.19→0.60µs），无新增计时器与重绘行。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)、[pi-tui-enhancements 包架构页](../architecture/packages/pi-tui-enhancements.md)。
  证据：`packages/pi-tui-enhancements/src/panel/quick-panel-editor.ts`、`src/panel/thinking-state.ts`、`src/panel/index.ts`、`src/display/compact-footer.ts`、`test/quick-panel-editor.test.ts`、`test/compact-footer.test.ts`。

- 2026-09-20 · [演进] `pi-tui-enhancements` 的快捷面板编辑器接管内置工作状态：working、重试、压缩、摘要等状态显示在输入框上边框内，不再占用输入框上方的独立状态行。范围：package:pi-tui-enhancements、context:tui-experience
  原因：Pi 只在编辑器暴露 `embedWorkingStatus === true` 且实现 `setWorkingStatusIndicator` 时才走内嵌路径；扩展替换编辑器后缺少该选项，Pi 会回退为独立状态行。用户对比两种位置后确认内嵌更合适，因此显式启用内嵌，并保留 `setWorkingVisible(true)` 让指示器保持可见。构造函数同时改为显式字段而非参数属性，以便 `node --test` 直接导入该类（Pi 运行时用 jiti，两种写法均可）。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)。
  证据：`packages/pi-tui-enhancements/src/panel/quick-panel-editor.ts`、`src/display/index.ts`、`test/quick-panel-editor.test.ts`。

- 2026-09-20 · [缺陷] `pi-tui-enhancements` 的折叠命令在流式生成期间保持固定单行高度：多行命令只显示第一行；参数仍在输入时仅多行命令用 `(N lines)` 显示输入行数，参数完成后先移除计数，产生输出后再用同一 `(N lines)` 样式显示输出行数，截断超宽命令时不得打断工具行背景。范围：package:pi-tui-enhancements、context:tui-experience
  原因：Pi 会在每段工具参数到达时重绘；原压缩逻辑先完整渲染多行命令，再裁正文却保留全部尾部空行，导致每生成一个换行时工具行高度在 4/5 行之间反复跳动。固定单行摘要并只保留一个结构性底部空行可消除闪烁；输入与输出计数按生命周期切换且不增加 `cmd`/`out` 标签，避免工具行过长并保持原有视觉样式。折叠命令超宽时曾用 `truncateToWidth` 裁剪，而该函数会在截断处插入 `\x1b[0m`，工具行背景由 Pi 的 Box 铺在整行外层，行内一个完整 reset 就让省略号、行数后缀和尾部空白掉回应用底色，形成半行深色块；改用 `sliceByColumn` 自行裁剪、省略号与后缀在无 reset 的纯文本上拼接后背景才能整行连续。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)。
  证据：`packages/pi-tui-enhancements/src/display/tool-rendering.ts`、`src/display/tool-policy.ts`、`test/tool-rendering.test.ts`。

- 2026-09-20 · [缺陷] 五个扩展包按 Pi 0.86 重建编译与验证基线，内部 agent loop 的系统提示改为 transcript 首条 system message：`pi-advisor`、`pi-observational-memory` 的 Pi 开发依赖升到 0.86，`pi-optimization`、`pi-tui-enhancements` 补上 `tsconfig.json` 与 `typecheck`/`check` 脚本，并同步修复 `ProviderHeaders` 可空、`thinkingLevel` 可空和 `visualTotal()` 漏调用。范围：workspace
  原因：Pi 0.86 的 `AgentContext` 不再含 `systemPrompt`，顾问循环与观察/反思/精简 worker 继续写入会静默丢失系统提示；四个包此前没有类型检查入口，Pi 契约变化只能靠运行时暴露。
  当前依据：[Pi 运行时契约](../architecture/shared/pi-runtime-contract.md)。
  证据：`packages/pi-advisor/src/advisor-runner.ts`、`packages/pi-observational-memory/src/agents/agent-context.ts`、`packages/pi-tui-enhancements/src/panel/quick-panel.ts`、各包 `package.json`、`tsconfig.json`。

- 2026-09-20 · [缺陷] `pi-lsp-feedback` 修复语言服务器并发启动留下的孤儿进程：同一 `<server id>:<root>` 的在途启动被复用，`close()` 等待在途启动落定，关闭检查与客户端登记移入同一同步块，已关闭的主客户端不再登记启动中的 TypeScript sidecar。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：同步预算超时后，下一次编辑会在前一个客户端完成初始化前再次进入 `getClient`；两个客户端都启动后后者覆盖前者，被覆盖的客户端不再出现在映射中，任何 `close()` 都不再关闭它，其子进程在宿主进程退出后继续存活（测试进程间歇性无法退出）。关闭期间完成的启动（含 Vue sidecar）也可能登记进已关闭的客户端，同样逃逸关闭流程。
  当前依据：[pi-lsp-feedback 架构页](../architecture/packages/pi-lsp-feedback.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)。
  证据：`packages/pi-lsp-feedback/src/diagnostic-service.js`（`getClient`、`startClient`、`close`）、`packages/pi-lsp-feedback/src/lsp-client.js`（`initialize`、`close`）、`packages/pi-lsp-feedback/test/diagnostic-service.test.mjs`、`test/lsp-client.test.mjs`。

- 2026-09-18 · [功能] `pi-tui-enhancements` 支持 Command Code 订阅额度：provider 名称匹配 `commandcode`/`command-code`/`cmdc` 时用官方 API key 调用官方 credits 与订阅接口，在面板和 footer 显示 5 小时、周和套餐月窗口的剩余百分比与重置时间；套餐未知时只省略月窗口。范围：package:pi-tui-enhancements、context:tui-experience
  原因：Command Code 订阅与 Codex/OpenCode Go 一样有独立的 5 小时/周/套餐限额，官方 API key 可直接查询，用户需要在 Pi 中观察同一订阅状态；Pi 中 provider 名称由用户自定义，必须按名称特征识别而不是绑定固定 id。
  当前依据：[pi-tui-enhancements 包架构页](../architecture/packages/pi-tui-enhancements.md)、[TUI 体验上下文](../requirements/contexts/tui-experience.md)。
  证据：`packages/pi-tui-enhancements/src/provider-usage.ts`、`packages/pi-tui-enhancements/test/provider-usage.test.ts`、`https://api.commandcode.ai/alpha/billing/credits`、`https://api.commandcode.ai/alpha/billing/subscriptions`。

- 2026-09-17 · [缺陷] 修复三个扩展的热路径缺陷：`pi-tui-enhancements` 的 `/context` 不再逐帧重算整页折行、footer 不再逐帧全量求和；`pi-lsp-feedback` 的编辑后诊断改为有预算的同步等待加后台收尾，并修掉关闭期间重启语言服务器导致的孤儿进程；`pi-observational-memory` 的覆盖进度改为增量水位。范围：package:pi-tui-enhancements、package:pi-lsp-feedback、package:pi-observational-memory、context:tui-experience、context:lsp-feedback、context:observational-memory
  原因：审计实测出三处可复现的资源与卡顿问题。`/context` 的 `renderContent` 每帧重建全部视觉行，滚动/搜索没有改变折行结果却仍付全量代价，实测 80k 行上下文每次按键约 12–21 秒；footer 在 Thinking 动画的 125ms 重绘里对全部会话条目求和。`pi-lsp-feedback` 在 `tool_result` 上 `await checkFile`，而 Pi 的 `afterToolCall` 是 await 该钩子的，导致每次编辑后 agent 循环被诊断等待拖住（普通发布上限 2.5 秒，首次 TypeScript 语义诊断 30 秒）；`DiagnosticService.close()` 先清空 clients 再等待关闭，启动中的客户端会在清理之后被写入 map，留下无人持有的语言服务器进程。观察式记忆的 `anyStageDue` 每个回合重建全账本索引并重新累计覆盖点之后的全部 token。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)、[观察式记忆上下文](../requirements/contexts/observational-memory.md)、[pi-tui-enhancements 包架构页](../architecture/packages/pi-tui-enhancements.md)、[pi-lsp-feedback 包架构页](../architecture/packages/pi-lsp-feedback.md)、[pi-observational-memory 包架构页](../architecture/packages/pi-observational-memory.md)。
  证据：`packages/pi-tui-enhancements/src/context/scrollable-tab.ts`（`VisualLineIndex`）、`src/display/compact-footer.ts`（`collectUsage` 增量缓存）、`packages/pi-lsp-feedback/extensions/index.js`（`settleWithinBudget`、`backgroundChecks`）、`src/diagnostic-service.js`（`closed` 与 `adopt`）、`packages/pi-observational-memory/src/session-ledger/token-progress.ts`；探针位于 `.tmp/perf-probe/`（未跟踪，可丢弃）。

- 2026-09-17 · [功能] 为 `pi-tui-enhancements` 增加会话级临时技能包：全局 `~/.pi/agent/skill-packs` 与项目 `.pi/skill-packs` 按目录分组，可通过 `/skill-packs` 一次启用或禁用整包技能。范围：package:pi-tui-enhancements、context:tui-experience
  原因：不常用技能不应进入常驻 `skills/` 或要求用户逐个切换；包选择需要只影响当前会话且不污染全局/项目设置；路由型包还需要只暴露入口目录，让总控技能按需读取子技能。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)、[pi-tui-enhancements 包架构页](../architecture/packages/pi-tui-enhancements.md)。
  证据：`packages/pi-tui-enhancements/src/panel/skill-pack-discovery.ts`、`src/panel/skill-packs.ts`、`skill-pack.json` manifest、Pi `resources_discover` 与 `ExtensionCommandContext.reload()`。

- 2026-09-14 · [演进] `pi-tui-enhancements` 的折叠工具行正文上限从两行收紧到一行，并且只在折叠态裁剪；`Ctrl+O` 或点击展开后输出完整结果；`powershell` 同时纳入紧凑结果集合。范围：package:pi-tui-enhancements、context:tui-experience
  原因：原实现无条件把可分组工具行裁到两行，`Ctrl+O` 展开大输出时只剩标题和一行结果（甚至算严重缺陷）；同时折叠态不需要保留第二行。展开态改由 Pi 决定输出量，裁剪只负责折叠态的单行外观。`powershell` 原本保留 Pi 原生结果预览，一行上限下折叠态会连预览一起丢掉且没有行数摘要，因此与 `bash` 保持一致改为紧凑后缀。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)、[pi-tui-enhancements 包架构页](../architecture/packages/pi-tui-enhancements.md)。
  证据：`packages/pi-tui-enhancements/src/display/tool-policy.ts`、`packages/pi-tui-enhancements/src/display/tool-rendering.ts`、`packages/pi-tui-enhancements/test/tool-rendering.test.ts`。

- 2026-09-14 · [功能] 将 npm 上的 `pi-context-inspector` 取回 `pi-tui-enhancements`，新增 `/context` 上下文查看弹窗；界面改为中文、沿用快捷面板的弹窗边框与宽度计算、移除 Nerd Font 图标，并从全局设置移除 `npm:pi-context-inspector`。范围：package:pi-tui-enhancements、context:tui-experience
  原因：单独安装的上下文查看器与 TUI 增强包职责重叠，需与紧凑 footer 的上下文展示保持同一所有者；原弹窗用字符数补齐、不裁剪超宽行，中文标签和长行会撑破边框换行，网格与分类图标使用 Nerd Font 私用区字形，无对应字体时显示为乱码。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)、[pi-tui-enhancements 包架构页](../architecture/packages/pi-tui-enhancements.md)。
  证据：`packages/pi-tui-enhancements/extensions/index.ts`、`packages/pi-tui-enhancements/src/context/`、`packages/pi-tui-enhancements/test/context-format.test.ts`、`C:\Users\xiakn\.pi\agent\settings.json`。

- 2026-09-14 · [缺陷] 修复 `pi-observational-memory` 在会话替换/重载后把旧 ctx/pi 的 stale 错误当作阶段失败并提示警告：新增会话代次，`session_shutdown` 时作废在途后台任务并释放运行标志，stale-ctx 错误按正常取消处理。范围：package:pi-observational-memory、context:observational-memory
  原因：Pi 在会话替换（new/resume/fork）、重载或退出时先触发 `session_shutdown`，再让旧 ctx/pi 失效；后台整理任务在失效后仍调用 `getContextUsage`/`appendEntry` 会抛 stale 错误，扩展把它当成阶段失败向用户提示。
  当前依据：[观察式记忆上下文](../requirements/contexts/observational-memory.md)、[pi-observational-memory 包架构页](../architecture/packages/pi-observational-memory.md)。
  证据：`packages/pi-observational-memory/src/runtime.ts`、`src/index.ts`、`src/hooks/consolidation-trigger.ts`、`src/hooks/compaction-trigger.ts`、`tests/runtime.test.ts`、`tests/consolidation-trigger.test.ts`、`tests/compaction-trigger.test.ts`。

- 2026-09-14 · [演进] `pi-observational-memory` 全包中文化：面向用户的通知、命令描述与输出、状态/查看内容、三个 worker 的提示词与工具元数据、源码注释全部改为简体中文，代码/路径/命令/标识符/错误消息保留原文。范围：package:pi-observational-memory、context:observational-memory
  原因：该包沿用上游英文通知与提示词，与仓库其他扩展的中文界面不一致；提示词中文化同时让写入压缩上下文的观察与反思内容为中文。
  当前依据：[观察式记忆上下文](../requirements/contexts/observational-memory.md)、[pi-observational-memory 包架构页](../architecture/packages/pi-observational-memory.md)。
  证据：`packages/pi-observational-memory/src/`、`packages/pi-observational-memory/tests/`。

- 2026-09-09 · [重构] 将 `pi-quick-panel` 与 `pi-lean-tool-display` 合并为 `pi-tui-enhancements`，并将 `pi-wait` 并入 `pi-optimization`；保留原有命令、快捷键和生命周期行为。范围：package:pi-tui-enhancements、package:pi-optimization、context:tui-experience、context:pi-optimization
  原因：两个 TUI 包共享运行时和 provider usage，分开安装容易重复注册；`pi-wait` 能力小且符合低干扰优化工具箱边界。
  当前依据：[TUI 体验上下文](../requirements/contexts/tui-experience.md)、[Pi 优化上下文](../requirements/contexts/pi-optimization.md)、[pi-tui-enhancements 包架构页](../architecture/packages/pi-tui-enhancements.md)、[pi-optimization 包架构页](../architecture/packages/pi-optimization.md)。
  证据：`packages/pi-tui-enhancements/extensions/index.ts`、`packages/pi-tui-enhancements/src/display/`、`packages/pi-tui-enhancements/src/panel/`、`packages/pi-tui-enhancements/src/provider-usage.ts`、`packages/pi-optimization/extensions/index.ts`、`packages/pi-optimization/src/wait.ts`。

- 2026-09-09 · [功能] 为 `pi-advisor` 增加 `skipWhenCurrentModel` 配置：当前执行模型命中裸模型 ID、精确 `provider/model` 或 glob 时，不注册或调用顾问。范围：package:pi-advisor、context:pi-advisor
  原因：当执行模型与顾问模型相同或用户明确使用更强模型时，额外顾问调用没有收益；跨 provider 无可靠的模型强弱排序，因此采用显式规则。
  当前依据：[pi-advisor 包架构页](../architecture/packages/pi-advisor.md)、[顾问上下文](../requirements/contexts/pi-advisor.md)。
  证据：`packages/pi-advisor/src/advisor-config.ts`、`packages/pi-advisor/src/advisor.ts`、`packages/pi-advisor/test/advisor-config.test.ts`、`packages/pi-advisor/test/advisor-skip.test.ts`。

- 2026-09-09 · [演进] 增加仓库根 `install-extensions.mjs`，默认只操作当前项目 `.pi/settings.json`，只清理设置中位于脚本目录下的本地扩展，再重新安装当前 `packages/` 中声明 `pi.extensions` 的包；需用显式 `--global` 才操作用户设置，其他来源扩展始终保留。范围：workspace
  原因：扩展改名、合并或新增后，避免旧本地路径与新包并存；真实 Pi 目录视为生产环境，只有用户明确要求时才执行安装。
  当前依据：无。
  证据：`install-extensions.mjs`、`README.md`、`.codestable/attention.md`。

- 2026-09-08 · [缺陷] `pi-optimization` 的 `nul` 重定向修复在包含任意 Here-doc/Here-string 脚本时仍处理脚本正文之外的独立重定向，避免 Git Bash 将 `>nul` 创建为普通文件。范围：package:pi-optimization、context:pi-optimization
  原因：改写入口只针对 Pi 直接执行的 `bash`/`user_bash`，`write`/`edit` 传入的脚本文本保持原样；原实现遇到运行命令中的脚本输入就整条命令跳过，导致脚本命令前后安全范围内的 `>nul` 漏过，现在只跳过脚本正文，继续扫描命令和脚本结束后的 Shell 代码。
  当前依据：[Pi 优化上下文](../requirements/contexts/pi-optimization.md)。
  证据：`packages/pi-optimization/src/nul-redirect.ts`、`packages/pi-optimization/test/nul-redirect.test.ts`。

- 2026-09-03 · [功能] 为本地 `pi-advisor` 增加可选的顾问内诊断工具：顾问可自行调用有界 `read`/`bash` 获取证据，但不会被强制触发，也不会获得 `edit`/`write` 工具。范围：package:pi-advisor、context:pi-advisor
  原因：用户需要顾问在必要时自行定位问题和运行诊断命令，但不接受“未运行测试”的强制提醒或自动流程。
  当前依据：[pi-advisor 包架构页](../architecture/packages/pi-advisor.md)、[顾问上下文](../requirements/contexts/pi-advisor.md)。
  证据：`packages/pi-advisor/src/advisor-runner.ts`、`packages/pi-advisor/src/advisor-tools.ts`、`packages/pi-advisor/src/advisor.ts`、`packages/pi-advisor/test/advisor-runner.test.ts`、`packages/pi-advisor/test/advisor-tools.test.ts`、`packages/pi-advisor/test/advisor-lifecycle.test.ts`。

- 2026-09-03 · [缺陷] 移除 `pi-advisor` 的自动“代码已修改但未验证”状态栏提醒；工具活动只保留给顾问上下文和阶段判断，advisor 不自动运行项目命令。范围：package:pi-advisor、context:pi-advisor
  原因：用户只需要专家定位和反馈，不需要测试/验证提示或任何自动命令引导；原提示既持续干扰，又不能证明验证是否成功。
  当前依据：[pi-advisor 包架构页](../architecture/packages/pi-advisor.md)、[顾问上下文](../requirements/contexts/pi-advisor.md)。
  证据：`packages/pi-advisor/src/advisor.ts`、`packages/pi-advisor/src/advisor-signals.ts`、`packages/pi-advisor/README.md`、`C:\\Users\\xiakn\\.pi\\agent\\settings.json`。

- 2026-09-03 · [功能] 在 `own-pi` 新增按原版行为实现的 `pi-advisor`，为复杂任务提供可选战略顾问调用，并将“修改后未验证”状态栏提醒限制为每个 agent run 一次、下一次工具结果或 run 结束时清除。范围：package:pi-advisor、context:pi-advisor
  原因：现有外部 advisor 扩展会在每个工具结果事件重复重写相同状态，造成底部状态栏持续干扰；需要保留原版顾问能力但修正提醒生命周期。
  当前依据：[pi-advisor 包架构页](../architecture/packages/pi-advisor.md)、[顾问上下文](../requirements/contexts/pi-advisor.md)。
  证据：`packages/pi-advisor/extensions/index.ts`、`packages/pi-advisor/src/advisor.ts`、`packages/pi-advisor/src/advisor-signals.ts`、`packages/pi-advisor/test/advisor-signals.test.ts`。

- 2026-09-02 · [功能] `pi-lean-tool-display` 和 `pi-quick-panel` 支持 `opencode-go` 订阅额度：通过官方 `/zen/go/v1/usage` API 显示 5 小时、周和月窗口的剩余百分比及重置信息，并保留 Codex 的两窗口展示。范围：package:pi-lean-tool-display、package:pi-quick-panel、context:lean-tool-display、context:quick-panel
  原因：OpenCode Go 订阅拥有独立于 Codex 的三窗口限额，官方 API key 可直接查询，用户需要在 Pi 中观察同一订阅状态。
  当前依据：[pi-lean-tool-display 包架构页](../architecture/packages/pi-lean-tool-display.md)、[pi-quick-panel 包架构页](../architecture/packages/pi-quick-panel.md)、[紧凑显示上下文](../requirements/contexts/lean-tool-display.md)、[快捷面板上下文](../requirements/contexts/quick-panel.md)。
  证据：`packages/pi-lean-tool-display/src/codex-usage.ts`、`packages/pi-quick-panel/src/codex-usage.ts`、`https://opencode.ai/zen/go/v1/usage`。

- 2026-09-01 · [功能] 为 `pi-optimization` 增加 fullscreen 鼠标滚轮优化：原生 Windows 默认将每次 Pi fullscreen 滚轮事件按 3 行处理，支持 `/fullscreen-scroll` 与 `settings.json` 中的 `fullscreen-scroll` 段即时调整或关闭；只在 Pi 暴露预期运行时 seam 时启用，并在会话关闭时恢复。范围：package:pi-optimization、context:pi-optimization
  原因：Pi 0.84 的 `TuiAltScreen` 默认 `wheelScrollLines` 为 1，ConPTY + PowerShell 7 下 fullscreen 滚轮明显慢于终端原生滚动；Pi ExtensionAPI 尚未公开该参数。
  当前依据：[pi-optimization 包架构页](../architecture/packages/pi-optimization.md)、[Pi 优化上下文](../requirements/contexts/pi-optimization.md)。
  证据：`packages/pi-optimization/src/fullscreen-scroll.ts`、`packages/pi-optimization/extensions/index.ts`、`packages/pi-optimization/test/fullscreen-scroll.test.ts`、Pi `@earendil-works/pi-tui` 的 `TuiAltScreen.routeWheel`。

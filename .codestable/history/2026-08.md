# 2026-08

- 2026-08-31 · [演进] 为 `pi-lsp-feedback` 增加 Vue TypeScript sidecar bridge：包内携带 `@vue/typescript-plugin` 与 `typescript-language-server`，转发 Volar 的 `tsserver/request`，并将当前快照的 `semanticDiagnosticsSync` 与 SFC 诊断合并；sidecar 启动、预热或请求失败时降级为 parser-only，bridge 请求使用独立上限。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：standalone Volar 可能把 Vue `<script setup lang="ts">` 的 TypeScript 语义错误报告成 confirmed 空诊断；扩展应自行携带桥接依赖，不能要求用户项目修改依赖或让可选能力故障阻断主 LSP。
  当前依据：[Vue TypeScript bridge ADR](../requirements/adrs/2026-08-vue-typescript-bridge.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)、[pi-lsp-feedback 包架构页](../architecture/packages/pi-lsp-feedback.md)。
  证据：`packages/pi-lsp-feedback/src/servers.js`、`src/diagnostic-service.js`、`src/lsp-client.js`、`test/fake-lsp.mjs`、`test/lsp-client.test.mjs`、`test/diagnostic-service.test.mjs`。

- 2026-08-31 · [缺陷] 修复 `pi-lsp-feedback` 在 Wails 工作区漏报 Go/TypeScript 诊断：LSP 客户端处理项目初始化所需的可选客户端请求，接受当前文档更新后收到的无版本 `publishDiagnostics`，为 React 文件选择正确语言 ID，并在 TypeScript 语言服务器支持时用 `semanticDiagnosticsSync` 主动完成冷启动诊断，消费同步结果或随后推送。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：`gopls` 和 TypeScript 语言服务器可能省略诊断版本；Wails 前端的 TypeScript 项目冷启动与首轮语义诊断超过原 2.5 秒推送等待上限，且把 `.tsx` 当作普通 TypeScript 会产生 `1005` 等伪诊断，导致真实结果漏报或污染反馈。
  当前依据：[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)、[pi-lsp-feedback 包架构页](../architecture/packages/pi-lsp-feedback.md)。
  证据：`packages/pi-lsp-feedback/src/lsp-client.js`、`src/servers.js`、`test/lsp-client.test.mjs`、`test/diagnostic-service.test.mjs`、`E:/github/git-monorepo-tools` 中 `main.go` 与 `src/main.tsx` 的实际诊断验证。

- 2026-08-31 · [演进] 为 `pi-lsp-feedback` 增加有界 LSP 文档状态缓存：客户端不再保留文档正文，最多跟踪 128 个最近使用且无等待者的文件 URI，淘汰时发送 `textDocument/didClose` 并清除诊断、版本和语言服务尝试状态，关闭或服务器退出时清空全部状态。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：长期 Pi 会话检查大量文件时，按 URI 无界保留完整文本和诊断会造成内存随文件数增长；保留少量近期协议状态可以维持诊断正确性，同时给驻留进程设置明确上限。
  当前依据：[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)、[pi-lsp-feedback 包架构页](../architecture/packages/pi-lsp-feedback.md)。
  证据：`packages/pi-lsp-feedback/src/lsp-client.js`（`touchDocument`、`evictDocument`、`clearTrackingState`）、`test/lsp-client.test.mjs`（有界状态和重新打开回归）。

- 2026-08-31 · [演进] 确认五个 Pi 包的长期产品主线：`pi-lean-tool-display` 负责视觉减负和美观、简洁、优雅的呈现；`pi-quick-panel` 负责便捷地查看筛选技能并切换模型；`pi-lsp-feedback` 只在反馈可能有益时发送诊断，同一问题即使文件被修改仍不重复发送；`pi-optimization` 作为收纳低干扰优化能力的工具箱；`pi-wait` 将 Pi 接收消息与 AI 执行时间解耦。范围：package:pi-lean-tool-display、context:lean-tool-display、package:pi-quick-panel、context:quick-panel、package:pi-lsp-feedback、context:lsp-feedback、package:pi-optimization、context:pi-optimization、package:pi-wait、context:pi-wait
  原因：这些包共同服务于低噪声、可控、便捷的 Pi 使用方式；未来判断新能力归属、反馈是否发送或界面是否保留时，需要优先遵守各包的体验中心和反目标。
  当前依据：[架构索引](../architecture/INDEX.md)、[需求上下文](../requirements/CONTEXT.md) 及各包架构页和领域上下文。
  证据：各包 README、入口与实现；本次已确认的产品主线。

- 2026-08-30 · [缺陷] 修复 `pi-wait` 到期任务不派发：定时器不再把不同事件回调的 `ExtensionContext` 做对象引用比较，改用会话代次判断活动生命周期。范围：package:pi-wait、context:pi-wait
  原因：Pi 为不同生命周期事件创建的上下文对象可能不是同一引用；旧实现到点执行 `ctx !== activeContext` 后直接返回，导致任务一直留在页脚且只能手动发送。
  当前依据：[pi-wait 包架构页](../architecture/packages/pi-wait.md)、[Pi 定时消息上下文](../requirements/contexts/pi-wait.md)。
  证据：`packages/pi-wait/src/wait.ts`、`packages/pi-wait/test/wait.test.ts`（不同事件上下文对象回归）。

- 2026-08-30 · [功能] 新增 `pi-wait` 定时消息包：`/wait <时间>` 捕获普通输入，或用 `--` 一行保存任务，到期后才以 follow-up 用户消息发送并展开斜杠命令；待发任务仅存于活动 Pi 进程内存，重载、切换会话和退出时清除。范围：package:pi-wait、context:pi-wait
  原因：额度暂不可用时，需要先输入并保留任务，但等待期间必须完全不调用 AI；Pi 公开 input 生命周期无法在已注册斜杠命令之前拦截，因此斜杠任务明确使用一行形式。选择内存而非伪持久化，避免新会话尚未创建 session 文件、非事务写入和跨会话误发造成错误承诺。
  当前依据：[pi-wait 包架构页](../architecture/packages/pi-wait.md)、[Pi 定时消息上下文](../requirements/contexts/pi-wait.md)。
  证据：`packages/pi-wait/src/wait.ts`、`packages/pi-wait/README.md`、`packages/pi-wait/test/`。

- 2026-08-28 · [演进] `pi-lean-tool-display` 将 `edit` 与 `write` 一并作为连续工具分组的独立边界，保留两者上下的显示间距。范围：package:pi-lean-tool-display、context:lean-tool-display
  原因：`edit` 和 `write` 都可能展示多行文件变更内容，与相邻工具调用黏连会削弱结果边界；两者应保持一致的独立显示。
  当前依据：[紧凑显示上下文](../requirements/contexts/lean-tool-display.md)。
  证据：`packages/pi-lean-tool-display/src/tool-rendering.ts`、`packages/pi-lean-tool-display/README.md`。

- 2026-08-26 · [功能] `pi-quick-panel` 在当前模型为 Codex 官方账号时异步显示 5 小时与周限额的剩余比例及重置时间/日期；非 Codex 或 usage 不可用时保持原面板并静默隐藏。范围：package:pi-quick-panel、context:quick-panel
  原因：快捷面板需要补充 UI 优化扩展已有的 Codex 限额信息，但重置窗口属于可选信息，不能延迟或阻断面板打开。
  当前依据：[pi-quick-panel 包架构页](../architecture/packages/pi-quick-panel.md)、[快捷面板上下文](../requirements/contexts/quick-panel.md)。
  证据：`packages/pi-quick-panel/src/codex-usage.ts`、`src/quick-panel.ts`、`src/quick-panel-ui.ts`、`test/codex-usage.test.ts`。

- 2026-08-26 · [功能] `pi-optimization` 接管 Pi 自带的扩展包更新检查：插件检查与启动并行，按调用栈隐藏重复提示，并在无窗口、无标准流、脱离父进程引用的独立 runner 中只更新实际有变化的扩展。范围：package:pi-optimization、context:pi-optimization
  原因：按读取次数设置闸门会被包管理器对每个扩展的内部读取提前消耗并导致重复提示；Pi 的 `update --extensions` 会把没有变化的扩展也纳入更新，因此改为调用栈闸门和按结果逐个执行 `update --extension <source>`，不增加轮询、常驻进程或定时器。
  当前依据：[pi-optimization 包架构页](../architecture/packages/pi-optimization.md)、[Pi 优化上下文](../requirements/contexts/pi-optimization.md)。
  证据：`packages/pi-optimization/src/auto-extension-update.ts`、`extensions/index.ts`、`test/auto-extension-update.test.ts`。

- 2026-08-26 · [缺陷] `pi-lean-tool-display` 同时解析 Codex usage 的 5 小时与周窗口，并在状态栏显示两者的剩余百分比。范围：package:pi-lean-tool-display、context:lean-tool-display
  原因：usage 接口已返回 `secondary_window` 周限额，但实现只读取 `primary_window` 并将状态文本硬编码为 5h。
  当前依据：[紧凑显示上下文](../requirements/contexts/lean-tool-display.md)、[pi-lean-tool-display 包架构页](../architecture/packages/pi-lean-tool-display.md)。
  证据：`packages/pi-lean-tool-display/src/codex-usage.ts`、`packages/pi-lean-tool-display/test/codex-usage.test.ts`。

- 2026-08-25 · [演进] `pi-lsp-feedback` 对未确认诊断和解析级联增加内容快照稳定门禁：首次结果只保留候选，只有同一文件内容与诊断语义再次一致才反馈；检查期间文件变化的结果直接丢弃。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：代理编辑过程中的瞬时 TSX 解析级联会污染上下文；上下文纯净度优先于诊断覆盖率，无法确认时宁可静默。
  当前依据：[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)、[pi-lsp-feedback 包架构页](../architecture/packages/pi-lsp-feedback.md)。
  证据：`packages/pi-lsp-feedback/src/feedback.js`、`src/diagnostic-service.js`、`extensions/index.js`、相关回归测试。

- 2026-08-24 · [演进] `pi-lean-tool-display` 接管 Pi footer 的紧凑布局：宽屏用 `|` 把仓库/分支、统计和右对齐模型分成三组，窄屏才拆成两行；仓库改为末级目录名，cache hit 改用方括号，统计值使用低饱和近似主题色区分且输入/输出共用 `thinkingLow`，并隐藏 cache read token 数、金额与 `(auto)`，扩展状态继续独立显示。范围：package:pi-lean-tool-display、context:lean-tool-display
  原因：默认 footer 无论宽度都把工作区和统计拆开，完整路径、cache read 数、金额与自动压缩标记在常用宽屏中占用空间但行动价值较低；少量强分隔配合克制的近似色，比逐项堆叠符号或高对比配色更容易扫描。
  当前依据：[pi-lean-tool-display 包架构页](../architecture/packages/pi-lean-tool-display.md)、[紧凑显示上下文](../requirements/contexts/lean-tool-display.md)。
  证据：`packages/pi-lean-tool-display/src/compact-footer.ts`、`extensions/index.ts`、`test/compact-footer.test.ts`。

- 2026-08-24 · [演进] `pi-lean-tool-display` 折叠思考时彻底移除 thinking 内容、标签和占位行，并以 footer 摘要行中统计与模型之间的实时标记替代转录区内的重复 `Thinking...`；标记尾部圆点每秒按 0～3 个循环，在工具执行和回合结束时隐藏，窄屏缩为单个圆点。范围：package:pi-lean-tool-display、context:lean-tool-display
  原因：每轮思考标签会把连续工具输出切开，footer 独立状态行右侧又不够醒目，而常驻浮层会遮挡转录内容并引入实验性 Overlay 生命周期；摘要行中统计与模型之间的位置能持续表达当前模型活动且不破坏工具阅读流。
  当前依据：[pi-lean-tool-display 包架构页](../architecture/packages/pi-lean-tool-display.md)、[紧凑显示上下文](../requirements/contexts/lean-tool-display.md)。
  证据：`packages/pi-lean-tool-display/src/message-display.ts`、`src/compact-footer.ts`、`extensions/index.ts`、`test/compact-footer.test.ts`。

- 2026-08-21 · [缺陷] 修复 `vision-mcp-auto` 的重复启动提示：保留 UI 通知，移除与其内容相同的 stdout 日志；`auto` 模式在模型尚未确定时等待同步，命令模式同步不再额外重复提示。范围：package:pi-optimization、context:pi-optimization
  原因：Pi 启动时会分别渲染 stdout 和 UI 通知，同一条识图状态被同时显示在顶部和底部；模型尚未确定时再同步临时状态还会放大重复感知。
  当前依据：[pi-optimization 包架构页](../architecture/packages/pi-optimization.md)、[Pi 优化上下文](../requirements/contexts/pi-optimization.md)。
  证据：`packages/pi-optimization/src/vision-mcp-auto.ts`、`packages/pi-optimization/test/vision-mcp-auto.test.ts`。

- 2026-08-21 · [功能] 新增 `pi-optimization` 私有 Pi 包，将 `fix-nul-redirect` 与 `vision-mcp-auto` 合并分发，并让两个功能独立加载、独立降级。范围：package:pi-optimization、context:pi-optimization
  原因：原扩展散落在用户目录，且视觉 MCP 与手动 Bash 后端属于可选外部能力，不应让一个功能或依赖故障影响另一个功能。
  当前依据：[pi-optimization 包架构页](../architecture/packages/pi-optimization.md)、[Pi 优化上下文](../requirements/contexts/pi-optimization.md)。
  证据：`packages/pi-optimization/`、`README.md`、`test/`。

- 2026-08-21 · [演进] 建立 Pi 扩展可选依赖软失败契约：缺少外部插件、peer dependency、运行时 API 或可选包内功能时，只跳过相关逻辑或 UI，不阻断同包功能、其他包或 Pi 启动。范围：workspace
  原因：插件之间存在逻辑依赖时，依赖消失不应把整个扩展包变成启动错误。
  当前依据：[可选扩展依赖](../architecture/shared/optional-extension-dependencies.md)、`.codestable/attention.md`。
  证据：`packages/pi-optimization/extensions/index.ts`、`packages/pi-quick-panel/extensions/index.ts`、`packages/pi-lean-tool-display/extensions/index.ts`。

- 2026-08-20 · [功能] 新增 `pi-lean-tool-display` 私有 Pi 包，将原全局紧凑显示扩展纳入仓库并按入口、消息显示、工具渲染和 Codex usage 分层；同时把临时 usage 验证脚本整理为正式回归测试。范围：package:pi-lean-tool-display、context:lean-tool-display
  原因：原插件位于用户目录且入口文件已不在当前目录中，无法随项目 Git 跟踪；显示补丁、生命周期和外部 usage 轮询混在散落文件中，维护和验证成本较高。
  当前依据：[包架构页](../architecture/packages/pi-lean-tool-display.md)、[紧凑显示上下文](../requirements/contexts/lean-tool-display.md)。
  证据：`packages/pi-lean-tool-display/`、`README.md`、`packages/pi-lean-tool-display/test/codex-usage.test.ts`。

- 2026-08-20 · [功能] 新增 `pi-quick-panel` 私有 Pi 包，将原全局 `quick-panel` 插件纳入仓库并按入口、面板编排、TUI 组件、配置和技能资源分层。范围：package:pi-quick-panel、context:quick-panel
  原因：原插件位于用户目录，无法随项目 Git 跟踪；散落在同一目录的入口、业务编排和 UI 代码也不利于维护。
  当前依据：[包架构页](../architecture/packages/pi-quick-panel.md)、[快捷面板上下文](../requirements/contexts/quick-panel.md)。
  证据：`packages/pi-quick-panel/`、`README.md`、`packages/pi-quick-panel/test/skills.test.ts`。

- 2026-08-20 · [演进] `pi-lsp-feedback` 按工具调用绑定反馈轮次，并对同一文件未变化的语义诊断做会话级去重；反馈策略移入 `FeedbackTracker`，避免迟到结果和重复错误污染代理上下文。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：连续编辑同一文件会重复注入相同 LSP 诊断，异步迟到结果还可能在无新编辑的后续轮次出现；生命周期适配与反馈策略混在入口模块中也增加了维护成本。
  当前依据：[包架构页](../architecture/packages/pi-lsp-feedback.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)。
  证据：`extensions/index.js`、`src/feedback.js`、`test/extension.test.mjs`、`README.md`。

- 2026-08-19 · [演进] `pi-lsp-feedback` 受信任项目覆盖配置改为一次读取、校验和合并，返回服务器清单与可显示问题；未知服务器、未知字段或字段类型错误不再静默失效。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：配置写错时用户无法发现覆盖未生效，容易误以为自定义服务器已启用。
  当前依据：[包架构页](../architecture/packages/pi-lsp-feedback.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)。
  证据：`src/config.js`、`src/servers.js`、`extensions/index.js`、`test/config.test.mjs`、`test/extension.test.mjs`。

- 2026-08-18 · [演进] `pi-lsp-feedback` 遵循“有则报、无则静默”：内置服务器在缺少项目标记时不再回退到工作区根，TypeScript/JavaScript 在解析不到 `@types/node` 时判为不可用；无类型/项目环境处（裸目录、未装 `@types/node` 的非工程包）不再反馈 2307/7006/2580 一类“缺环境”诊断噪音。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：无类型环境的位置会让 tsserver 对 `node:*` 内置模块报大量“找不到类型声明”及连锁隐式 any 噪音，占据无意义代理上下文；文件级 LSP 只沿目录向上解析 `node_modules/@types`，全局 `npm i -g` 装 `@types/node` 也不生效。
  当前依据：[包架构页](../architecture/packages/pi-lsp-feedback.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)。
  证据：`src/servers.js`（`fallbackToWorkspace`、`needsNodeTypes`、`findNodeTypesRoot`）、`src/diagnostic-service.js`（`DiagnosticService.checkFile`）、`test/diagnostic-service.test.mjs`、`test/extension.test.mjs`。

- 2026-08-18 · [演进] `pi-lsp-feedback` 只向代理反馈实际的 LSP 错误和警告，未确认、不可用和干净状态不再注入上下文。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：诊断可用性和新鲜度不是待代理处理的源码问题，状态提示会制造无效行动。
  当前依据：[包架构页](../architecture/packages/pi-lsp-feedback.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)。
  证据：`packages/pi-lsp-feedback/extensions/index.js`、`test/extension.test.mjs`、`README.md`。

- 2026-08-17 · [演进] `pi-lsp-feedback` 将 Node 语言服务器改为包内分发，并在可信项目中托管安装缺失的 `gopls`。范围：package:pi-lsp-feedback、context:lsp-feedback
  原因：项目不应为使用诊断扩展而重复安装语言服务器。
  当前依据：[包架构页](../architecture/packages/pi-lsp-feedback.md)、[LSP 反馈上下文](../requirements/contexts/lsp-feedback.md)。
  证据：`packages/pi-lsp-feedback/package.json`、`src/servers.js`、`src/diagnostic-service.js`、`src/managed-server-installer.js`。
